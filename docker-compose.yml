version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:3000"
    volumes:
      - ./server/data:/app/server/data
    environment:
      - NODE_ENV=production
      - JWT_SECRET=conquerors-lobby-secret-777
      - OPENAI_API_KEY=literouter
    restart: always
